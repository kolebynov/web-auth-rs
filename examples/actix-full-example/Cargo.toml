[package]
name = "actix-full-example"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
actix-web = { version = "4" }
tokio = { version = "1.48", features = ["full"] }
web-auth-rs = { path = "../../", features = ["actix", "jwt"] }
